# NOTE: Enable remote control so external programs (e.g. sketchybar) can query Kitty.
# The 'listen_on' directive below is ignored unless this is set to 'yes', 'socket', or 'socket-only'.
allow_remote_control yes

# NOTE: Create a UNIX socket for remote control. This lets external scripts (like
# sketchybar) run `kitten @ --to unix:/tmp/kitty-<pid>` and retrieve titles.
# The actual socket will be created as /tmp/kitty-<pid>.
listen_on unix:/tmp/kitty

# Kitty theme
# Add the theme at the top, any setting below the theme will be overriden
# include ~/github/dotfiles-latest/kitty/themes/themes/ChallengerDeep.conf
include ~/github/dotfiles-latest/kitty/themes/themes/Catppuccin-Mocha.conf
# include ~/github/dotfiles-latest/kitty/themes/themes/Linkarzu.conf
# include ~/github/dotfiles-latest/kitty/active-theme.conf
#include ~/github/dotfiles-latest/kitty/themes/themes/solarized_osaka_dark.conf

# xterm-kitty is the default, but I'm hardcoding it just in case
term xterm-kitty

font_size 15

# font_family      JetBrainsMono Nerd Font
font_family SF Mono
# font_family SF Mono Square
# bold_font SF Mono Square Bold
# bold_italic_font SF Mono Square BoldItalic
#font_family      JetBrainsMono Nerd Font Mono
modify_font cell_width 93%
modify_font cell_height 5px

# font_family JetBrains Nerd Font
# fallback_family UDEV Gothic NF

# 特定のユニコード範囲を特定のフォントに割当
symbol_map U+E0A0-U+E0A3,U+E0C0-U+E0C7 PowerlineSymbols
symbol_map U+3000-U+9FFF UDEV Gothic NF

scrollback_lines 10000

# This assumes you installed tmux through brew if using macos
# shell zsh --login -c "if command -v tmux >/dev/null 2>&1; then tmux attach || tmux; else zsh; fi"
shell zsh --login


# When you do fn+ctrl+shift+2 it opens the kitty file in the editor below
editor nvim

hide_window_decorations titlebar-only
# hide_window_decorations titlebar-and-corners

window_padding_width 5 5 5 5 
# window_padding_width 0

# I'm adding these below the theme in case the theme sets them
cursor #ebfafa
cursor_text_color #0D1116

confirm_os_window_close 0

enable_audio_bell no

macos_option_as_alt right

cursor_shape beam
cursor_beam_thickness 1.5
cursor_underline_thickness 2.0
cursor_blink_interval 0
cursor_stop_blinking_after 0
cursor_trail 1
cursor_trail_decay 0.1 0.3

cursor_trail_start_threshold 2

background_opacity 0.7
background_blur 20

tab_bar_edge top
tab_bar_style powerline
tab_bar_filter session:~ or session:^$
tab_title_template "{tab.active_wd.rsplit('/', 1)[-1]}"
# tab_bar_style hidden

#
# Keybindings
kitty_mod cmd+alt

# map ctrl+shift+s save_as_session --use-foreground-process --relocatable
# map ctrl+b>o goto_session $HOME/.config/kitty/sessions/obsidian.kitty-session
# map ctrl+b>h goto_session $HOME/.config/kitty/sessions/home.kitty-session
# map ctrl+b>b goto_session $HOME/.config/kitty/sessions/blogpost.kitty-session
# map ctrl+b>p goto_session $HOME/.config/kitty/sessions/dots-private.kitty-session

# map ctrl+b>s goto_session
# map ctrl+a>l goto_session -1
#
# map kitty_mod+u goto_layout tall
# map kitty_mod+j goto_layout fat
# map kitty_mod+m goto_layout stack
# map cmd+t new_tab_with_cwd

# map kitty_mod+f5 load_config_file
map kitty_mode+r   load_config_file

# map kitty_mod+enter new_window
# map cmd+enter new_window
# map ctrl+l next_window
# map ctrl+h previous_window
# #: Close window
# map kitty_mod+w close_window
map kitty_mod+k kitten keymap.py
#
    # kitty_scrollback_nvim
    allow_remote_control socket-only
    listen_on unix:/tmp/kitty
    shell_integration enabled

    # nvim --headless +'KittyScrollbackGenerateKittens'
    # ----->8-------->8-------->8--------->8-------
    # kitty-scrollback.nvim Kitten alias
    action_alias kitty_scrollback_nvim kitten ~/.local/share/nvim/lazy/kitty-scrollback.nvim/python/kitty_scrollback_nvim.py
    
    # Browse scrollback buffer in nvim (ctrl + shift + h)
    map kitty_mod+h kitty_scrollback_nvim
    
    # Browse output of the last shell command in nvim
    #map kitty_mod+g kitty_scrollback_nvim --config ksb_builtin_last_cmd_output
    
    # Show clicked command output in nvim
    #mouse_map ctrl+shift+right press ungrabbed combine : mouse_select_command_output : kitty_scrollback_nvim --config ksb_builtin_last_visited_cmd_output
#
# Ctrl+Shift+h : browse nvim
nvim --headless +'KittyScrollbackGenerateKittens'
    # -----8<--------8<--------8<---------8<-------
